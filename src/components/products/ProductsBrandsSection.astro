---
// Sección de marcas: paso 2 del flujo (Categoría → Marca → Productos).
// El script inyecta las cards en #brands-grid y controla visibilidad de #brands-section.
---

<section id="brands-section" class="mb-8 hidden" aria-label="Elige una marca">
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-6">
    <div>
      <p class="text-sm font-medium text-[#5d3fbb] mb-1">Paso 2 de 2</p>
      <h2 class="text-xl sm:text-2xl font-bold text-gray-900">Elige una marca</h2>
      <p class="text-gray-600 text-sm sm:text-base mt-1">Encuentra tus marcas favoritas en esta categoría.</p>
    </div>
    <button
      type="button"
      id="back-to-categories-btn"
      class="inline-flex items-center gap-2 px-4 py-2.5 rounded-xl border-2 border-[#5d3fbb] text-[#5d3fbb] hover:bg-[#5d3fbb] hover:text-white font-semibold text-sm transition-all duration-200 shrink-0"
      aria-label="Volver a categorías"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
      </svg>
      Volver a categorías
    </button>
  </div>
  <div
    id="brands-grid"
    class="grid grid-cols-3 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-3 sm:gap-4 lg:gap-6"
    role="list"
  >
    <!-- Las marcas se renderizan aquí por el script de la página -->
  </div>
</section>
